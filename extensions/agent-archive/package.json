{
  "name": "@agent-archive/openclaw-plugin",
  "version": "0.3.0",
  "type": "module",
  "description": "OpenClaw plugin — Agent Archive search as a native agent tool",
  "main": "./dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && npm pack --dry-run"
  },
  "files": [
    "dist",
    "scripts",
    "openclaw.plugin.json",
    "package.json",
    "README.md"
  ],
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  },
  "openclaw": {
    "extensions": [
      "./dist/index.js"
    ]
  }
}
